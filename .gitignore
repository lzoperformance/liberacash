.idea
.DS_Store
libs/

# Credenciais — NUNCA commitar (aprenda com o erro do credito.vc:
# esses arquivos estavam aqui listados mas já tinham sido commitados
# antes, então o git continuava versionando. Se algum dia existir um
# desses arquivos de verdade, rode `git rm --cached <arquivo>` também).
config/db-config.php
config/aws-config.php
config/parceiros-config.php
config/anthropic-config.php
.env
.env.*
!.env.example

# Dependências do Composer (AWS SDK etc.)
vendor/

# Capturas de referência do design system (41MB, material de consulta de um
# case de terceiros, não usado em produção) — fica só local, não no histórico
# do git. Ver design-system/readme.md. O resto do design-system (tokens,
# componentes, guidelines, ui_kits) é versionado normalmente.
design-system/shots/
public/images/hero-mulher-nova-original.png
