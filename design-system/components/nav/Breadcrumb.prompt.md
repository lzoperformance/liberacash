Slash-separated path above a screen figure. Used on the wallet screen: `Overview / Balance Details`.

```jsx
<Breadcrumb items={['Overview', 'Balance Details']} />
```

Separator is a typographic `/`, never a chevron icon.
