Miniature payment-card thumbnail for card pickers and transaction rows.

```jsx
<CardTile scheme="visa" tone="green" width={56} />
<CardTile scheme="mastercard" tone="dark" width={22} />
```

Purely cosmetic — 22px wide in table rows, 56px in the wallet card list. Real scheme logos should
replace the stand-in wordmark in production.
