Capsule meter — spending limits, finance score, savings goals.

```jsx
<ProgressBar value={45} tone="split" label="Spending Limits" caption="$4,500.00 spent of $10,000.00" />
```

Always carries its baseline in `caption` — a bare percentage tells the user nothing.
