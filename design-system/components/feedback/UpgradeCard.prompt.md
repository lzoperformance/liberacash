The soft upsell that lives at the foot of the Sidebar. Centred icon tile, title, quoted body,
full-width dark button.

```jsx
<UpgradeCard onAction={openPlans} />
```

Copy stays soft and states the payoff, never a countdown or a discount badge.
