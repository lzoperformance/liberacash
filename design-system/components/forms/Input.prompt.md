Text field. The one component in the system that is NOT a capsule — 12px soft-rect
(`--radius-field`), so it reads as an editable surface rather than a button.

```jsx
<Input label="You send" value="$200.00" suffix="USD" onChange={fn} />
<Input icon="mail" placeholder="you@email.com" error="That address looks wrong" />
```
