Settings toggle. Green when on, grey when off; the knob slides, it does not bounce.

```jsx
<Switch checked={on} onChange={fn} label="Unusual transaction alerts"
  description="Notify me when a charge looks duplicated or out of pattern." />
```
