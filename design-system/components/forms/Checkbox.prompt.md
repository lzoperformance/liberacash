Row-selection checkbox for the transactions table. 18px, 5px radius, forest fill with a green tick.

```jsx
<Checkbox checked={sel} onChange={toggle} />
```

This is the one place the system allows a small radius — a capsule checkbox would read as a radio.
