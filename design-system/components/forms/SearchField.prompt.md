The topbar search — a capsule with a magnifier and the ⌘/F shortcut chips.

```jsx
<SearchField width={320} />
```

The ⌘ and F characters are typographic, one of only two places unicode stands in for an icon
(see readme > ICONOGRAPHY).
