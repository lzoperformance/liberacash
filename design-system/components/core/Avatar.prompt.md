Round account avatar with an initials fallback on forest-100.

```jsx
<Avatar name="Jenny Wilson" size="md" ring />
```

No photography ships with this system, so `src` is usually left empty and the initials show.
`ring` is reserved for the topbar account button.
